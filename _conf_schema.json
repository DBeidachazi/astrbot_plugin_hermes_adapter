{
  "hermes_profile": {
    "type": "string",
    "description": "Hermes 内部 profile 名称，例如 default 或 xcq；QQ 会话隔离由插件自动处理",
    "default": "default"
  },
  "hermes_gateway_url": {
    "type": "string",
    "description": "Hermes 网关地址（profile 未提供时使用）",
    "default": "http://host.docker.internal:8642"
  },
  "hermes_gateway_auth_token": {
    "type": "string",
    "description": "网关访问密钥；建议直接填写。留空仅在 AstrBot 进程已配置环境变量时生效",
    "default": ""
  },
  "timeout": {
    "type": "int",
    "description": "网关单次读取空闲超时（秒），建议 300；working 消息会刷新该计时",
    "default": 300
  },
  "admin_qq_ids": {
    "type": "list",
    "description": "允许使用 /h 的管理员 QQ 列表",
    "items": { "type": "string" },
    "default": []
  },
  "admin_qq_id": {
    "type": "string",
    "description": "兼容单个管理员 QQ 配置",
    "default": ""
  }
}
